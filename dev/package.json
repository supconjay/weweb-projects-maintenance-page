{
  "name": "projects-list-harness",
  "private": true,
  "version": "0.0.0",
  "description": "Standalone Vite harness for the projects-list section. Kept out of the root package.json on purpose: WeWeb's importer installs the root devDependencies and only accepts @weweb/cli there.",
  "scripts": {
    "dev": "vite"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7",
    "vue": "^3.5.13"
  }
}
